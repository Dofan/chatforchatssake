// World Module for server

// create a 2d array

// Create a world object that will hold everything needed and will
// be returned when this module is referenced
var world = {};


var world_grid = [];
for (var y = 0; y < 128; y ++){
	console.log(y);
}


module.exports = world;